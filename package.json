{
  "name": "bumper-tanks",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "balance": "npm run build && node tools/balance-brawl.mjs",
    "maps": "npm run build && node tools/map-check.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
